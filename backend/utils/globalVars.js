const POST_FILTERS = ["new", "top"]
const POST_CATEGORIES = ["all", "event", "discussion", "donation"]

const ORG_CATEGORIES = ["health", "education", "environment", "humanitarian"]
const ORG_STATUS = ["all", "approved", "pending"]
const ORG_FILTERS = ["new", "status"]

const MAX_TEXT_LEN = 256
const MAX_LONG_TEXT_LEN = 2048

const USER_ROLE = ["user", "moderator", "administrator"]

module.exports = {
    POST_FILTERS,
    POST_CATEGORIES,
    ORG_CATEGORIES,
    ORG_STATUS,
    ORG_FILTERS,
    MAX_TEXT_LEN,
    MAX_LONG_TEXT_LEN,
    USER_ROLE
}